# Rust
/target/
**/*.rs.bk
Cargo.lock

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Secrets / local config
*.local
.env.*
!.env.example

# Local PKI (CA + leaf keys never leave the dev host)
infra/tls-local/

# Brainstorm scratch (visual companion)
.superpowers/

# Editor / OS
.DS_Store
.idea/
.vscode/

# serena MCP tool cache
.serena/

# Git worktrees
/.worktrees/

# Local MCP client config (bearer tokens)
.mcp.json
.claude/settings.local.json
.direnv/
coverage/
dist/
build/
*.agekey
*.decrypted.*

# pytest tmp_path basetemp, created when the suite runs from the service dir
pytest-of-*/
